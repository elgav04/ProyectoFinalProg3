import { FiltertipovehiculoPipe } from './filtertipovehiculo.pipe';

describe('FiltertipovehiculoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertipovehiculoPipe();
    expect(pipe).toBeTruthy();
  });
});
