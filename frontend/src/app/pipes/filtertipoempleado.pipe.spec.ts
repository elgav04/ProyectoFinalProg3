import { FiltertipoempleadoPipe } from './filtertipoempleado.pipe';

describe('FiltertipoempleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertipoempleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
