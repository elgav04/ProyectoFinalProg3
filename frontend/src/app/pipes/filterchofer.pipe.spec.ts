import { FilterchoferPipe } from './filterchofer.pipe';

describe('FilterchoferPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterchoferPipe();
    expect(pipe).toBeTruthy();
  });
});
