import { FiltercargaPipe } from './filtercarga.pipe';

describe('FiltercargaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercargaPipe();
    expect(pipe).toBeTruthy();
  });
});
