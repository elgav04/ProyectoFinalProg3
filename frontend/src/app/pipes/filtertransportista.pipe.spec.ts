import { FiltertransportistaPipe } from './filtertransportista.pipe';

describe('FiltertransportistaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertransportistaPipe();
    expect(pipe).toBeTruthy();
  });
});
