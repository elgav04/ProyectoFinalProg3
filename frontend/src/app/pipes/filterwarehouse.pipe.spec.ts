import { FilterwarehousePipe } from './filterwarehouse.pipe';

describe('FilterwarehousePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterwarehousePipe();
    expect(pipe).toBeTruthy();
  });
});
