import { FilterempleadoPipe } from './filterempleado.pipe';

describe('FilterempleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterempleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
