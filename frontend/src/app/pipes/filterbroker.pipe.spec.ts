import { FilterbrokerPipe } from './filterbroker.pipe';

describe('FilterbrokerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbrokerPipe();
    expect(pipe).toBeTruthy();
  });
});
