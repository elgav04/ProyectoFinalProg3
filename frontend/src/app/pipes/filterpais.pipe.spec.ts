import { FilterpaisPipe } from './filterpais.pipe';

describe('FilterpaisPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpaisPipe();
    expect(pipe).toBeTruthy();
  });
});
